public class elevator {
    boolean elevatorstages = false;

    public void elevatorextends() {
        if (elevatorstages); {
            elevatorstages = true;
            System.out.println("elevator extended");
        }
    }
    //Make a functin called toggle that extends if elevator is in and pulls in when out
}
